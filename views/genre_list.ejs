<%- include("./partials/header.ejs") %>

<h1><%= title %></h1>

<% if (genre_list.length) { %>
    <ul>
      <% genre_list.forEach(genre => { %>
        <li><a href="<%= genre.url %>"><%= genre.name %></a></li>
      <% }) %> 
    </ul>
<% } else { %>
  <p>There are no books, so there aren't any book genres…</p>
<% } %>

<%- include("./partials/footer.ejs") %>